# A DNS Server

This repo is a DNS Server built with *Go* that utilizes a common NEXTSERVER address to pass unknown queries to a trusting DNS server and caches the response for later requests.

Built during the course of my Computer Networks class.
